---
slug: <slug>
status: in_progress
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
---
# SLUG — `<slug>` 開發狀態記錄

> 生命週期:`IN_PROGRESS → PASSED`。規範詞彙依 SBM-SKILL §1。狀態與內容分離(見 SBM-SKILL §4 #4)。

## 1. 目標(`<slug>` 弧:跨迭代穩定)

(秘書 MUST 填入目標,源自老闆意圖。屬長程注意力弧——`<nnn>` 內細節變動不動此處;目標變更才回老闆改這裡。意圖釐清區塊深化結論以當下事實整合於此。)

## 2. 兩個注意力弧的狀態

`<slug>`(長程弧)與 `<nnn>`(短程弧)分開追蹤;混淆即注意力失準(見 SBM-GATE §3)。

**`<slug>` 狀態(長程)**:`IN_PROGRESS → PASSED`(老闆拍板結束)。

**`<nnn>` 狀態(短程,標籤非閘)**:`迭代中 → 穩定 → 執行 → <complete>`,任一時刻可退回迭代中:

| `<nnn>` | 狀態 | 老闆指示 | 備註 |
|---------|------|----------|------|
| 001 | 迭代中 | — | 初始 epoch |

推進 MUST 由老闆指示。**決策歸哪個弧**:問「會改變跨迭代穩定的東西嗎」——是→`<slug>`(回老闆);否→`<nnn>`。

## 3. 研究與迭代(`<nnn>` 弧:當輪結論)

三軌角色迭代修正 G1/G2/G3 聚合快照直到穩定(見 SBM-SKILL §3);DEVELOPER 開發軌(causal)。

| `<nnn>` | 研究重點 | 視角 |
|---------|----------|------|
| 001 | (秘書 MUST 填入) | 需求/技術/風險/成本/合規 |

## 4. 技術債清單

| 編號 | 來源 | 描述 | 建議 | 狀態 |
|------|------|------|------|------|

## 5. 推進與交接

- **推進**:`<nnn>` 序列逐項 commit 並達 `<complete>` 後,MUST 由老闆指示推進。
- **退回**:老闆「退回/撤銷」時產出保留 `.shiftblame/`,提交路由暫停。
- **交接摘要**:秘書 MUST 彙整最終結論 3~5 行白話寫入此處(收尾時填入)。

## 6. 租約有效期

臨時規範,三層租約的中期層。歸檔前 MUST 逐條標記處理狀態。

| 編號 | 描述 | 日期 | 處置(升級SOP / 隨歸檔失效) | 狀態 |
|------|------|------|------------------------------|------|
