# SEARXNG-mcp — 研究／搜尋工具

## 適用

外部資訊研究、官方文件查證、版本號、bundle size、業界做法查證。

## 能力

`searxng_web_search`、`web_url_read`、`searxng_instance_info`、`searxng_search_suggestions`。

## 使用

G1 查外部生態、業界先例、競品做法；外部查證先用內建 web，硬約束時升級 codex rescue。單一來源不據以 PASS。
