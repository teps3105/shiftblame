---
slug: <slug>
status: in_progress
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
revision: 8.6
---
# SLUG — `<slug>`

> 本檔保存跨循環不變的命題、授權邊界與目前所在節點；流程看 SBM-SKILL §0 主圖。
> G1／G2／G3 三權範本見本文末「三權範本」段——建立 `<nnn>` 時從該段複製對應範本到本檔的 §3 目前節點表下方展開。

## 1. 老闆原始命題

> （忠實引用老闆提出的問題或目標，不加入候選解法。）

## 2. SECRETARY 意圖揭露

| 欄位 | 內容 |
|------|------|
| 意圖翻譯 | |
| 邊界 | |
| 未知／待決 | |
| 已授權方案 | |
| 允許修改範圍 | |
| 老闆路由決定 | （逐字記錄老闆決定） |

## 3. 目前節點

| `<nnn>` | 老闆路由決定 | 節點 | 最近交付 | 退回原因 |
|---------|--------------|------|----------|----------|
| 001 | （老闆原話） | 三權制衡（G1↔G2↔G3） | — | — |

只有老闆明確決定開新 `<nnn>` 時才能新增一列；同一子需求的擴充更新原列。新 `<slug>` 也只能依老闆明確決定建立。SECRETARY MAY 基於脈絡提出開新 `<nnn>`／`<slug>` 的提議（SBM-SKILL §9），但 MUST NOT 在老闆授權前預建檔案或自行新增列。

本表是目前節點的唯一權威。合法節點與主圖一一對應：`三權制衡（G1↔G2↔G3）／開發／證據／三者重審／nnn 完成／老闆 PASS／收尾`。不得跳點；退回一律改回 `三權制衡（G1↔G2↔G3）` 後重走。

**`<nnn>` 完成 ≠ slug PASS**（SBM-SKILL §0、§1.4、§6、§1.7）：

| 節點 | 語義 | 誰觸發 | 後續 |
|------|------|--------|------|
| `nnn 完成` | 單一子需求循環收斂（三者重審通過、片段清空） | 三者重審通過即到達 | 先做輕量保鮮（§1.7.1），再由老闆決定開新 `<nnn>`（加新列）或結束 `<slug>` |
| `老闆 PASS` | 整個 `<slug>` 結束，老闆拍板 | 老闆在 `<nnn>` 完成（含輕量保鮮）後決定結束 `<slug>` | 完整收尾保鮮（§1.7.2）→ 移 archive/ |
| `收尾` | `<slug>` 已 PASS，執行完整保鮮與歸檔 | PASS 後進入 | 歸檔完成，`<slug>` 結束 |

老闆在同一 `<slug>` 內開新 `<nnn>` 時，舊 `<nnn>` 列節點定為 `nnn 完成` 即可，**不需先走 PASS／收尾**。frontmatter `status` 只記 `<slug>` 生命週期：建立時 `in_progress`，老闆 PASS 後改為 `passed`。

## 4. 目標與品質

| 項目 | 內容 |
|------|------|
| 目標 | |
| 業務品質 | |
| 明確不做 | |

## 5. 技術債

| 編號 | 來源 | 描述 | 建議 | 狀態 |
|------|------|------|------|------|

## 6. 臨時租約

| 編號 | 描述 | 日期 | 處置（升級 SOP／隨歸檔失效） | 狀態 |
|------|------|------|--------------------------------|------|

## 7. 交接摘要

（收尾時由 SECRETARY 以 3～5 行白話整理最終結果。）

---

## 三權範本（建立 `<nnn>` 時複製本檔展開）

> 以下為 G1／G2／G3 三份制衡文件的範本。建立新 `<nnn>` 時，從此處複製三段到本檔 §3 目前節點表下方，以 `## <nnn> G1`／`## <nnn> G2`／`## <nnn> G3` 展開填寫。三份各由主導者改寫，兩兩雙向一致（判準見 SBM-SKILL §10）才可開發。

### G1 範本 — 需求／驗收標準（AUDITOR 主導）

> AUDITOR 主導。與 G2、G3 兩兩雙向制衡；只寫需求／驗收面向，不寫技術選型或實作步驟。G1 只由 AUDITOR 改寫。

**1. 原始命題**

> （忠實引用 SLUG §1，不加入候選解法。）

**2. 需求結論**

| 需求 | 為什麼需要 | 成立時可觀察到 | 不成立時可觀察到 |
|------|------------|----------------|------------------|

每項需求 MUST 能由使用者觀察成立與不成立；「檔案存在」「字串出現」不是業務驗收條件。

**3. 邊界**

| 類型 | 內容 |
|------|------|
| 必須包含 | |
| 明確不做 | |
| 使用情境 | |
| 品質要求 | |

**4. 變更標的**

| 標的 | 需求層變更 | 動作 |
|------|------------|------|

**5. 未知與風險**

| 未知／風險 | 為何重要 | 建議處理 | 狀態 |
|-------------|----------|----------|------|

**6. 與 G2／G3 兩兩一致前檢查**

- [ ] 原始命題未混入秘書或開發者提出的解法。
- [ ] 每項需求都有可觀察的成立與不成立條件。
- [ ] 邊界與不做事項明確。
- [ ] codebase／外部事實附可查核來源。
- [ ] AUDITOR 已取得並複核需求層外部獨立研究、反證或替代觀點。
- [ ] **兩兩雙向一致（判準見 SBM-SKILL §10）**——逐項核對三對六向，全部成立：
  - [ ] G1↔G2：G1 每項需求在 G2 有對應技術分析；G2 每項技術分析能回指所承接的 G1 需求。
  - [ ] G1↔G3：G1 每項需求在 G3 有對應驗收操作；G3 每項驗收與實作步驟能回指所支持的 G1 需求。
  - [ ] G2↔G3：G3 每個實作步驟對應一項 G2 技術分析，且兩者指向同一 G1 需求；G2 每項技術分析在 G3 實作步驟中被落實，且與該步驟指向同一 G1 需求。

任一項未完成時留在三權制衡；不一致者調整自己主導的文件。

### G2 範本 — 技術分析（RESEARCHER 主導）

> RESEARCHER 主導。與 G1、G3 兩兩雙向制衡。G2 只由 RESEARCHER 改寫；與 G1 需求矛盾時，回三權制衡要求 AUDITOR 調整 G1，不在 G2 內改寫需求。

**1. 技術方案**

| 對應的 G1 需求 | 技術分析與做法 | 採用理由 | codebase／來源 |
|-----------------|----------------|----------|-----------------|

優先順序：跳過不需要的工作 → 復用既有 → 標準庫／平台原生 → 已裝依賴 → 最短直接實作。新依賴 MUST 說明必要性。

**2. 測試方式**

| 對應的 G1 需求 | Given 前置條件 | When 操作 | Then 可觀察結果 | 實作前預期失敗 |
|-----------------|----------------|-----------|-----------------|-----------------|

每項 G1 需求 MUST 至少有一個正常或異常情境；測試結果必須能支持 G1 的可觀察條件。

**3. 開發自驗**

| 對應的 G1 需求 | 自驗方式 | 通過條件 |
|-----------------|----------|----------|

自驗可使用測試、grep、字串、行數或檔案檢查，但只能證明實作完成度，不能取代 G3 的業務驗收。

**4. 技術風險**

| 風險 | 影響 | 預防／驗證方式 | 來源 |
|------|------|----------------|------|

**5. 與 G1／G3 兩兩一致前檢查**

- [ ] 每項技術方案都指出對應的 G1 需求。
- [ ] 每項 G1 需求都有測試方式。
- [ ] G2 沒有新增或改寫 G1 需求。
- [ ] 不確定事項已有查證或明確標示。
- [ ] RESEARCHER 已取得並複核技術層外部獨立研究、反證或替代觀點。
- [ ] **兩兩雙向一致（判準見 SBM-SKILL §10）**——逐項核對三對六向，全部成立：
  - [ ] G1↔G2：G1 每項需求在 G2 有對應技術分析；G2 每項技術分析能回指所承接的 G1 需求。
  - [ ] G1↔G3：G1 每項需求在 G3 有對應驗收操作；G3 每項驗收與實作步驟能回指所支持的 G1 需求。
  - [ ] G2↔G3：G3 每個實作步驟對應一項 G2 技術分析，且兩者指向同一 G1 需求；G2 每項技術分析在 G3 實作步驟中被落實，且與該步驟指向同一 G1 需求。

任一項未完成時留在三權制衡；不一致者調整自己主導的文件。

### G3 範本 — 驗收與開發計畫（DEVELOPER 主導）

> DEVELOPER 主導。與 G1、G2 兩兩雙向制衡。G3 依序完成兩件事：①回讀 G1 寫驗收；②驗收完整後才依 G2 寫計畫，不得倒序。G3 只由 DEVELOPER 改寫。

**1. 驗收條件（先填）**

| G1 原始需求 | 驗收操作 | 通過判準 | 需要的證據 | 狀態 |
|-------------|----------|----------|--------------|------|

規則：

- 每項 G1 需求 MUST 在表中出現，語意不得偷換。
- 驗收描述使用者可觀察的行為或結果，不以檔案、字串、grep 命中代替。
- 無法寫出操作、判準或證據時，MUST 回三權制衡；不得先寫下一節。

**2. 實作步驟（驗收完整後填）**

步驟按所屬功能分組；功能即多循環螺旋的一圈（SBM-SKILL §0、§1.4）。

| 順序 | 所屬功能 | 行動 | 對應的 G2 分析 | 支持的 G1 需求 | 依賴 |
|------|----------|------|-----------------|-----------------|------|

行動只能依賴已完成項。範圍外檔案不得列入或修改。開發以**功能**為提交與驗收單位：每個功能（可含多列步驟）完成後 DEVELOPER 先 commit，再進入階段驗收（見 §2.5）；不合格返工並疊加新 commit。G2 §3 的需求層級自驗於對應需求的所有步驟完成後另行執行。

**2.5 階段驗收記錄（多循環螺旋，每圈一列）**

| 功能 | 做了什麼 | 實現什麼功能 | 發生什麼事 | commit | 老闆確認 | AUDITOR 複驗（G1 回寫） | 狀態 |
|------|----------|--------------|------------|--------|----------|--------------------------|------|

- 「做了什麼／實現什麼功能／發生什麼事」由 DEVELOPER 完成 commit 後撰寫，描述使用者可觀察的行為，不以檔案、字串、grep 命中代替。
- 老闆確認該功能是否正常；AUDITOR 對照 G1 複驗並寫回 G1 驗收確認，依實作情況判斷是否要求 RESEARCHER 補 G2 或 DEVELOPER 補 G3（輕量補寫，不重跑制衡）。
- 狀態：待驗收／返工／合格。DEVELOPER 自驗（commit 前）發現並直接修正的錯誤不計入返工；獨立審核（含 DEVELOPER 自派子代理、AUDITOR、老闆）抓到的錯誤才標「返工」——輕微（Minor）返工疊加新 commit 重驗，重大（Major）回三權制衡。合格進入下一圈。

**3. 開發策略**

| 項目 | 結論 |
|------|------|
| 複雜度 | 單段／causal 小步 |
| 測試順序 | |
| 證據收集 | |
| commit 切分 | 每功能一提交（返工疊加新 commit）；此列填 commit 訊息命名策略（如 type 前綴） |

**4. 預期變更**

| 風險 | Minor／Major | 發生時回到哪裡 |
|------|-------------|----------------|

**5. 三權一致（開發）前檢查**

- [ ] 驗收表完整後才撰寫實作步驟。
- [ ] 每項 G1 需求都有驗收與實作對應。
- [ ] 驗收判準來自 G1，不是由 G2 實作細節反推。
- [ ] G3 沒有新增 G1 未授權的需求。
- [ ] **兩兩雙向一致（判準見 SBM-SKILL §10）**——逐項核對三對六向，全部成立：
  - [ ] G1↔G2：G1 每項需求在 G2 有對應技術分析；G2 每項技術分析能回指所承接的 G1 需求。
  - [ ] G1↔G3：G1 每項需求在 G3 有對應驗收操作；G3 每項驗收與實作步驟能回指所支持的 G1 需求。
  - [ ] G2↔G3：G3 每個實作步驟對應一項 G2 技術分析，且兩者指向同一 G1 需求；G2 每項技術分析在 G3 實作步驟中被落實，且與該步驟指向同一 G1 需求。

三份文件兩兩一致後才可開發；不一致者調整自己主導的文件。
