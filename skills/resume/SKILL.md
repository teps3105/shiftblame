---
name: resume
description: 核對既有授權、正式文件、工作樹與保存落點，從原階段接續未完成工作。
---
# 恢復工作

依 [主技能](../shiftblame/SKILL.md) 與 `sb state` 核對 repo、slug／ms、階段及已核准契約；讀取任務相關的正式文件。使用者已指定目標就接續，只有多個合理目標且無法由脈絡確定時才請其選擇。

有 `last_save` 時，讀 SLUG §8 指向 tmp 內的交接，核對實體路徑、commit、狀態與目前差異。成功才清除標記；缺檔、越界或矛盾時保留標記，由現有文件及 git 重建落點。重建後依 [save](../save/SKILL.md) 保存。

無交接時按原階段讀取已存在且適用的 G 檔與證據。保留仍有效的工作，修正過時技術描述；G1 定義偏離封存時先查明差異，需求改義仍須重新核准。

沿用既有 slug／ms、階段及授權完成工作，直接承接使用者的明確續行要求。範圍擴大與兩個決策時點仍由使用者判定。
